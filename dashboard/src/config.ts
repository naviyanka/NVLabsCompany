/**
 * Application-wide configuration.
 *
 * COMPANY_ID controls which company context the dashboard operates in.
 * This is a single-tenant prototype default; swap with a context provider
 * or route param when multi-tenancy is needed.
 */
export const COMPANY_ID = 'default';
